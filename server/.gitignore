# Audio yang di-generate server (cache) — jangan di-commit
audio-cache/

# API key & token server — rahasia
.env

# Database & konfigurasi runtime server (dibuat otomatis di VPS)
database.json
app-config.json
